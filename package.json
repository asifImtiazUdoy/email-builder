{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "cd artifacts/velour-email && PORT=5000 BASE_PATH=/ ../../node_modules/.bin/pnpm dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "start": "cd artifacts/velour-email && PORT=5000 BASE_PATH=/ ../../node_modules/.bin/pnpm preview",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
