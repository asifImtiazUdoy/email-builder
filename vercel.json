{
  "buildCommand": "pnpm install && cd artifacts/velour-email && pnpm run build",
  "devCommand": "pnpm dev",
  "installCommand": "corepack prepare pnpm@latest --activate && pnpm install",
  "framework": "vite",
  "env": {
    "PORT": "3000",
    "BASE_PATH": "/"
  }
}
